{
  "name": "hoptrail-web",
  "version": "0.0.0",
  "private": true,
  "description": "Hoptrail web UI — Svelte + Vite + uPlot. Builds into web/dist/ which is embedded into the Go binary at compile time.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^4.2.0",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "uplot": "^1.6.30"
  }
}
